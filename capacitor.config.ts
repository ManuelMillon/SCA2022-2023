import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'SCA2022-2023',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
